<figure class="metamorfozy-video-card" aria-label="Metamorfozy makijażu permanentnego Bogusława Herda">
  <video
    src="/video/metamorfozy-pmu-boguslawa-herda.mp4"
    poster="/video/metamorfozy-pmu-boguslawa-herda-poster.webp"
    autoplay
    muted
    loop
    playsinline
    preload="metadata"
  ></video>

  <figcaption>
    Metamorfozy PMU pokazane spokojnie — bez przypadkowej galerii, z naciskiem na naturalny efekt,
    światło i realny wygląd po zabiegu.
  </figcaption>
</figure>

<style>
  .metamorfozy-video-card {
    position: relative;
    width: min(100%, 320px);
    max-width: 320px;
    margin: 0 auto;
    overflow: hidden;
    padding: 7px;
    border-radius: 28px;
    background:
      radial-gradient(circle at 8% 14%, rgba(56, 187, 181, 0.10), transparent 13rem),
      radial-gradient(circle at 92% 88%, rgba(217, 146, 162, 0.12), transparent 14rem),
      rgba(255, 255, 255, 0.92);
    border: 1px solid rgba(56, 187, 181, 0.13);
    box-shadow: 0 18px 44px rgba(23, 21, 29, 0.08);
  }

  .metamorfozy-video-card::before {
    content: "";
    position: absolute;
    inset: 8px;
    z-index: 2;
    border-radius: 21px;
    border: 1px solid rgba(255, 255, 255, 0.44);
    pointer-events: none;
  }

  .metamorfozy-video-card video {
    display: block;
    width: 100%;
    height: auto;
    max-height: 520px;
    aspect-ratio: 9 / 16;
    object-fit: cover;
    object-position: center;
    border-radius: 21px;
    background: rgba(255, 255, 255, 0.72);
  }

  .metamorfozy-video-card figcaption {
    position: relative;
    z-index: 3;
    margin: 0;
    padding: 13px 11px 3px;
    color: var(--color-muted, #6e6670);
    font-size: 0.84rem;
    font-weight: 560;
    line-height: 1.5;
    text-align: center;
  }

  @media (min-width: 981px) {
    .metamorfozy-video-card {
      width: min(100%, 300px);
      max-width: 300px;
      align-self: center;
    }

    .metamorfozy-video-card video {
      max-height: 500px;
    }
  }

  @media (max-width: 760px) {
    .metamorfozy-video-card {
      width: min(100%, 82vw);
      max-width: 285px;
      margin: 20px auto 22px;
      padding: 6px;
      border-radius: 24px;
      box-shadow: 0 14px 34px rgba(23, 21, 29, 0.07);
    }

    .metamorfozy-video-card::before {
      inset: 7px;
      border-radius: 18px;
    }

    .metamorfozy-video-card video {
      max-height: 430px;
      border-radius: 18px;
    }

    .metamorfozy-video-card figcaption {
      padding: 12px 9px 2px;
      font-size: 0.8rem;
      line-height: 1.45;
    }
  }
</style>
