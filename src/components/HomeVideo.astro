<section class="home-video-section" aria-label="Powrót do natury w praktyce - film Bogusława Herda">
  <div class="container home-video-card">
    <div class="home-video-copy">
      <p class="section-kicker">Powrót do natury w praktyce</p>
      <h2>Naturalny efekt zaczyna się od spokojnego spojrzenia na twarz.</h2>
      <p>
        Makijaż permanentny nie powinien tworzyć nowej twarzy. Powinien wydobywać to,
        co już jest piękne: miękką linię, właściwy odcień, lepszą proporcję i świeżość,
        która nie dominuje nad urodą.
      </p>

      <div class="home-video-pills" aria-label="Filozofia pracy">
        <span>naturalny efekt</span>
        <span>bez przerysowania</span>
        <span>spokojna decyzja</span>
      </div>
    </div>

    <div class="home-video-frame">
      <video
        class="home-video"
        src="/video/boguslawa-herda-pmu-home-video-light.mp4"
        poster="/video/boguslawa-herda-pmu-home-poster.webp"
        autoplay
        muted
        loop
        playsinline
        preload="metadata"
        aria-label="Film wizerunkowy Bogusława Herda - naturalny makijaż permanentny Bielsko-Biała"
      ></video>
    </div>
  </div>
</section>

<style>
  .home-video-section {
    position: relative;
    overflow: hidden;
    padding: clamp(28px, 5vw, 72px) 0 clamp(52px, 7vw, 98px);
    background:
      radial-gradient(circle at 12% 18%, rgba(56, 187, 181, 0.10), transparent 24rem),
      radial-gradient(circle at 92% 82%, rgba(217, 146, 162, 0.12), transparent 26rem),
      linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(255, 248, 251, 0.74));
  }

  .home-video-card {
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 0.92fr) minmax(320px, 1.08fr);
    gap: clamp(24px, 4vw, 58px);
    align-items: center;
    padding: clamp(22px, 4vw, 48px);
    border-radius: clamp(30px, 4vw, 54px);
    background:
      radial-gradient(circle at 8% 12%, rgba(56, 187, 181, 0.12), transparent 18rem),
      radial-gradient(circle at 96% 92%, rgba(217, 146, 162, 0.12), transparent 20rem),
      rgba(255, 255, 255, 0.86);
    border: 1px solid rgba(56, 187, 181, 0.14);
    box-shadow: 0 28px 84px rgba(23, 21, 29, 0.08);
  }

  .home-video-copy h2 {
    max-width: 760px;
    margin: 0;
    color: var(--color-text, #17151d);
    font-size: clamp(2.25rem, 4.3vw, 5rem);
    font-weight: 780;
    line-height: 0.96;
    letter-spacing: -0.075em;
  }

  .home-video-copy p:not(.section-kicker) {
    max-width: 680px;
    margin: 24px 0 0;
    color: var(--color-muted, #6e6670);
    font-size: clamp(1rem, 1.35vw, 1.12rem);
    line-height: 1.72;
  }

  .home-video-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 28px;
  }

  .home-video-pills span {
    display: inline-flex;
    min-height: 36px;
    align-items: center;
    justify-content: center;
    padding: 0 14px;
    border-radius: 999px;
    color: var(--color-turquoise-dark, #0f7773);
    background: rgba(219, 246, 243, 0.72);
    border: 1px solid rgba(56, 187, 181, 0.16);
    font-size: 0.74rem;
    font-weight: 820;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .home-video-frame {
    position: relative;
    overflow: hidden;
    border-radius: clamp(28px, 4vw, 48px);
    background: rgba(255, 255, 255, 0.74);
    border: 1px solid rgba(217, 146, 162, 0.18);
    box-shadow: 0 28px 78px rgba(199, 119, 136, 0.13);
  }

  .home-video-frame::before {
    content: "";
    position: absolute;
    inset: 14px;
    z-index: 2;
    border-radius: clamp(22px, 3vw, 36px);
    border: 1px solid rgba(255, 255, 255, 0.42);
    pointer-events: none;
  }

  .home-video {
    display: block;
    width: 100%;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    object-position: center;
  }

  @media (max-width: 980px) {
    .home-video-card {
      grid-template-columns: 1fr;
    }

    .home-video-frame {
      order: 1;
    }

    .home-video-copy {
      order: 2;
    }
  }

  @media (max-width: 760px) {
    .home-video-section {
      padding: 26px 0 48px;
    }

    .home-video-card {
      padding: 20px;
      border-radius: 28px;
    }

    .home-video-frame {
      border-radius: 24px;
    }

    .home-video-frame::before {
      inset: 9px;
      border-radius: 18px;
    }

    .home-video {
      aspect-ratio: 16 / 9;
    }

    .home-video-copy h2 {
      font-size: clamp(2.05rem, 10vw, 3.1rem);
      line-height: 1.02;
      letter-spacing: -0.066em;
    }

    .home-video-pills span {
      width: 100%;
      justify-content: flex-start;
      min-height: 38px;
      font-size: 0.7rem;
    }
  }
</style>
