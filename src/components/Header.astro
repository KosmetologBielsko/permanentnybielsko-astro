---
const booksyUrl =
  "https://booksy.com/pl-pl/104871_boguslawa-herda-permanentny-make-up_salon-kosmetyczny_12680_bielsko-biala";

const currentPath = Astro.url.pathname;

const navLinks = [
  { label: "PMU", href: "/makijaz-permanentny-bielsko/" },
  { label: "Brwi", href: "/makijaz-permanentny-brwi/" },
  { label: "Usta", href: "/makijaz-permanentny-ust/" },
  { label: "Kreski", href: "/makijaz-permanentny-oczu/" },
  { label: "Usuwanie", href: "/usuwanie-makijazu-permanentnego/" },
  { label: "Szkolenia", href: "/szkolenie-makijaz-permanentny/" },
  { label: "Galeria", href: "/galeria/" },
  { label: "Cennik", href: "/cennik-makijaz-permanentny/" },
  { label: "Kontakt", href: "/kontakt/" }
];

const mobileQuickLinks = [
  { label: "Regulamin salonu", href: "/regulamin-salonu/" },
  { label: "Polityka prywatności", href: "/rodo-polityka-prywatnosci-regulamin/" }
];

const isActive = (href: string) => {
  if (href === "/") return currentPath === "/";
  return currentPath === href || currentPath === href.replace(/\/$/, "");
};
---

<header class="site-header" data-site-header>
  <div class="header-shell">
    <a class="header-logo" href="/" aria-label="Bogusława Herda Permanent Makeup — strona główna">
      <span class="header-logo-mark">BH</span>

      <span class="header-logo-copy">
        <strong>Bogusława Herda</strong>
        <em>Permanent Makeup</em>
      </span>
    </a>

    <nav class="desktop-nav" aria-label="Menu główne">
      {navLinks.map((link) => (
        <a href={link.href} aria-current={isActive(link.href) ? "page" : undefined}>
          {link.label}
        </a>
      ))}
    </nav>

    <div class="header-actions">
      <a class="header-booksy" href={booksyUrl} target="_blank" rel="noopener noreferrer">
        Umów wizytę
      </a>

      <button
        class="mobile-menu-button"
        type="button"
        aria-label="Otwórz menu"
        aria-expanded="false"
        aria-controls="mobile-menu-panel"
        data-menu-button
      >
        <span></span>
        <span></span>
      </button>
    </div>
  </div>

  <div class="mobile-menu-backdrop" data-menu-backdrop></div>

  <aside class="mobile-menu-panel" id="mobile-menu-panel" aria-label="Menu mobilne" data-mobile-menu>
    <div class="mobile-menu-head">
      <p>Menu</p>
      <span>Naturalny PMU • Bielsko-Biała</span>
    </div>

    <nav class="mobile-nav" aria-label="Menu mobilne">
      {navLinks.map((link, index) => (
        <a href={link.href} aria-current={isActive(link.href) ? "page" : undefined}>
          <span>{String(index + 1).padStart(2, "0")}</span>
          {link.label}
        </a>
      ))}
    </nav>

    <div class="mobile-menu-cta">
      <a href={booksyUrl} target="_blank" rel="noopener noreferrer">
        Umów wizytę przez Booksy
      </a>
    </div>

    <nav class="mobile-formal-links" aria-label="Linki formalne">
      {mobileQuickLinks.map((link) => (
        <a href={link.href}>{link.label}</a>
      ))}
    </nav>
  </aside>
</header>

<script>
  const header = document.querySelector("[data-site-header]");
  const button = document.querySelector("[data-menu-button]");
  const panel = document.querySelector("[data-mobile-menu]");
  const backdrop = document.querySelector("[data-menu-backdrop]");

  const closeMenu = () => {
    if (!header || !button) return;

    header.classList.remove("is-menu-open");
    button.setAttribute("aria-expanded", "false");
    button.setAttribute("aria-label", "Otwórz menu");
    document.documentElement.classList.remove("mobile-menu-open");
  };

  const openMenu = () => {
    if (!header || !button) return;

    header.classList.add("is-menu-open");
    button.setAttribute("aria-expanded", "true");
    button.setAttribute("aria-label", "Zamknij menu");
    document.documentElement.classList.add("mobile-menu-open");
  };

  button?.addEventListener("click", () => {
    if (header?.classList.contains("is-menu-open")) {
      closeMenu();
    } else {
      openMenu();
    }
  });

  backdrop?.addEventListener("click", closeMenu);

  panel?.querySelectorAll("a").forEach((link) => {
    link.addEventListener("click", closeMenu);
  });

  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape") {
      closeMenu();
    }
  });

  window.addEventListener("resize", () => {
    if (window.innerWidth > 1080) {
      closeMenu();
    }
  });
</script>

<style>
  .site-header {
    position: fixed;
    top: 18px;
    left: 0;
    right: 0;
    z-index: 1000;
    padding: 0 18px;
    pointer-events: none;
  }

  .header-shell {
    width: min(1480px, 100%);
    min-height: 74px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    gap: 18px;
    align-items: center;
    padding: 10px 12px 10px 14px;
    border: 1px solid rgba(183, 101, 118, 0.13);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.78);
    box-shadow: 0 20px 60px rgba(72, 42, 55, 0.12);
    backdrop-filter: blur(22px);
    pointer-events: auto;
  }

  .header-logo {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    min-width: 0;
    color: var(--color-text, #17151d);
    text-decoration: none;
  }

  .header-logo-mark {
    display: grid;
    width: 48px;
    height: 48px;
    flex: 0 0 auto;
    place-items: center;
    border-radius: 17px;
    color: #ffffff;
    font-size: 1.05rem;
    font-weight: 850;
    letter-spacing: -0.08em;
    background:
      radial-gradient(circle at 24% 24%, rgba(255, 255, 255, 0.3), transparent 3rem),
      linear-gradient(145deg, #9d4f68 0%, #d7899f 45%, #7f344d 100%);
    box-shadow: 0 16px 38px rgba(157, 79, 104, 0.22);
  }

  .header-logo-copy {
    display: grid;
    min-width: 0;
    gap: 2px;
  }

  .header-logo-copy strong {
    color: var(--color-text, #17151d);
    font-size: clamp(1rem, 1.35vw, 1.35rem);
    font-weight: 790;
    line-height: 1;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .header-logo-copy em {
    color: #b76576;
    font-size: 0.58rem;
    font-style: normal;
    font-weight: 730;
    line-height: 1;
    letter-spacing: 0.32em;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .desktop-nav {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 3px;
    min-width: 0;
  }

  .desktop-nav a {
    display: inline-flex;
    min-height: 42px;
    align-items: center;
    border-radius: 999px;
    padding: 0 clamp(8px, 0.75vw, 13px);
    color: rgba(23, 21, 29, 0.72);
    font-size: clamp(0.75rem, 0.74vw, 0.88rem);
    font-weight: 740;
    letter-spacing: -0.02em;
    text-decoration: none;
    white-space: nowrap;
    transition:
      background 0.2s ease,
      color 0.2s ease,
      transform 0.2s ease;
  }

  .desktop-nav a:hover,
  .desktop-nav a[aria-current="page"] {
    color: var(--color-text, #17151d);
    background: rgba(219, 246, 243, 0.72);
    transform: translateY(-1px);
  }

  .header-actions {
    display: inline-flex;
    justify-content: flex-end;
    align-items: center;
    gap: 10px;
  }

  .header-booksy {
    display: inline-flex;
    min-height: 48px;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    padding: 0 18px;
    color: #ffffff;
    font-size: 0.86rem;
    font-weight: 800;
    text-decoration: none;
    white-space: nowrap;
    background:
      radial-gradient(circle at 20% 18%, rgba(255, 255, 255, 0.24), transparent 4rem),
      linear-gradient(135deg, #b76576, #8f344d);
    box-shadow: 0 14px 36px rgba(183, 101, 118, 0.2);
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease;
  }

  .header-booksy:hover {
    transform: translateY(-1px);
    box-shadow: 0 18px 44px rgba(183, 101, 118, 0.24);
  }

  .mobile-menu-button {
    display: none;
    width: 48px;
    height: 48px;
    flex: 0 0 auto;
    place-items: center;
    border: 1px solid rgba(183, 101, 118, 0.16);
    border-radius: 999px;
    padding: 0;
    background: rgba(255, 255, 255, 0.68);
    cursor: pointer;
  }

  .mobile-menu-button span {
    display: block;
    width: 18px;
    height: 2px;
    border-radius: 999px;
    background: var(--color-text, #17151d);
    transition:
      transform 0.24s ease,
      opacity 0.24s ease;
  }

  .mobile-menu-button span + span {
    margin-top: -10px;
    transform: translateY(10px);
  }

  .is-menu-open .mobile-menu-button span:first-child {
    transform: translateY(5px) rotate(45deg);
  }

  .is-menu-open .mobile-menu-button span:last-child {
    transform: translateY(5px) rotate(-45deg);
  }

  .mobile-menu-backdrop {
    position: fixed;
    inset: 0;
    z-index: -2;
    opacity: 0;
    pointer-events: none;
    background: rgba(25, 18, 24, 0.26);
    backdrop-filter: blur(4px);
    transition: opacity 0.24s ease;
  }

  .mobile-menu-panel {
    position: fixed;
    top: 106px;
    right: 18px;
    z-index: -1;
    width: min(430px, calc(100vw - 36px));
    max-height: calc(100vh - 128px);
    overflow: auto;
    padding: 18px;
    border: 1px solid rgba(183, 101, 118, 0.16);
    border-radius: 32px;
    background:
      radial-gradient(circle at 94% 10%, rgba(56, 187, 181, 0.16), transparent 14rem),
      radial-gradient(circle at 10% 88%, rgba(201, 127, 143, 0.14), transparent 14rem),
      rgba(255, 255, 255, 0.9);
    box-shadow: 0 24px 80px rgba(72, 42, 55, 0.24);
    backdrop-filter: blur(22px);
    opacity: 0;
    pointer-events: none;
    transform: translateY(-10px) scale(0.98);
    transform-origin: top right;
    transition:
      opacity 0.24s ease,
      transform 0.24s ease;
    scrollbar-width: thin;
  }

  .is-menu-open .mobile-menu-backdrop {
    opacity: 1;
    pointer-events: auto;
  }

  .is-menu-open .mobile-menu-panel {
    opacity: 1;
    pointer-events: auto;
    transform: translateY(0) scale(1);
  }

  .mobile-menu-head {
    padding: 8px 8px 16px;
  }

  .mobile-menu-head p {
    margin: 0;
    color: var(--color-turquoise-dark, #0f7773);
    font-size: 0.72rem;
    font-weight: 840;
    text-transform: uppercase;
    letter-spacing: 0.2em;
  }

  .mobile-menu-head span {
    display: block;
    margin-top: 8px;
    color: var(--color-muted, #6e6670);
    font-size: 0.9rem;
    line-height: 1.45;
  }

  .mobile-nav {
    display: grid;
    gap: 8px;
  }

  .mobile-nav a {
    display: grid;
    grid-template-columns: 42px 1fr;
    gap: 12px;
    align-items: center;
    min-height: 56px;
    border: 1px solid rgba(56, 187, 181, 0.13);
    border-radius: 20px;
    padding: 0 16px;
    color: var(--color-text, #17151d);
    font-size: 1rem;
    font-weight: 780;
    text-decoration: none;
    background: rgba(255, 255, 255, 0.62);
  }

  .mobile-nav a[aria-current="page"] {
    background: rgba(219, 246, 243, 0.86);
  }

  .mobile-nav span {
    color: var(--color-turquoise-dark, #0f7773);
    font-size: 0.7rem;
    font-weight: 860;
    letter-spacing: 0.12em;
  }

  .mobile-menu-cta {
    margin-top: 14px;
  }

  .mobile-menu-cta a {
    display: flex;
    min-height: 56px;
    align-items: center;
    justify-content: center;
    border-radius: 20px;
    padding: 0 18px;
    color: #ffffff;
    font-size: 0.94rem;
    font-weight: 820;
    text-align: center;
    text-decoration: none;
    background:
      radial-gradient(circle at 20% 18%, rgba(255, 255, 255, 0.24), transparent 4rem),
      linear-gradient(135deg, #b76576, #8f344d);
    box-shadow: 0 14px 36px rgba(183, 101, 118, 0.18);
  }

  .mobile-formal-links {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 14px;
    padding: 0 2px 2px;
  }

  .mobile-formal-links a {
    display: inline-flex;
    min-height: 38px;
    align-items: center;
    border-radius: 999px;
    padding: 0 13px;
    color: var(--color-muted, #6e6670);
    font-size: 0.78rem;
    font-weight: 700;
    text-decoration: none;
    background: rgba(255, 255, 255, 0.58);
    border: 1px solid rgba(183, 101, 118, 0.12);
  }

  @media (max-width: 1320px) {
    .header-shell {
      gap: 10px;
    }

    .desktop-nav a {
      padding: 0 8px;
      font-size: 0.75rem;
    }

    .header-booksy {
      padding: 0 15px;
    }
  }

  @media (max-width: 1080px) {
    .site-header {
      top: 12px;
      padding: 0 12px;
    }

    .header-shell {
      min-height: 70px;
      grid-template-columns: auto auto;
      justify-content: space-between;
      border-radius: 26px;
      padding: 10px;
    }

    .desktop-nav {
      display: none;
    }

    .mobile-menu-button {
      display: grid;
    }

    .header-booksy {
      display: none;
    }
  }

  @media (max-width: 520px) {
    .header-logo-mark {
      width: 46px;
      height: 46px;
      border-radius: 16px;
    }

    .header-logo-copy strong {
      max-width: 220px;
      overflow: hidden;
      font-size: 0.98rem;
      letter-spacing: 0.06em;
      text-overflow: ellipsis;
    }

    .header-logo-copy em {
      font-size: 0.54rem;
      letter-spacing: 0.23em;
    }

    .mobile-menu-panel {
      top: 94px;
      right: 12px;
      width: calc(100vw - 24px);
      max-height: calc(100vh - 112px);
      border-radius: 28px;
      padding: 14px;
    }
  }

  @media (max-width: 390px) {
    .header-logo-copy strong {
      max-width: 178px;
      font-size: 0.9rem;
    }

    .header-logo-copy em {
      font-size: 0.5rem;
      letter-spacing: 0.18em;
    }
  }

  /* V2 — menu polish: desktop typography + centered mobile formal links */
  @media (min-width: 1081px) {
    .header-shell {
      min-height: 76px !important;
      padding: 10px 12px 10px 16px !important;
    }

    .desktop-nav {
      gap: 2px !important;
    }

    .desktop-nav a {
      min-height: 40px !important;
      padding: 0 clamp(9px, 0.68vw, 12px) !important;
      color: rgba(23, 21, 29, 0.66) !important;
      font-size: clamp(0.74rem, 0.68vw, 0.82rem) !important;
      font-weight: 690 !important;
      line-height: 1 !important;
      letter-spacing: 0.015em !important;
      text-transform: none !important;
      background: transparent !important;
    }

    .desktop-nav a:hover {
      color: var(--color-text, #17151d) !important;
      background: rgba(255, 255, 255, 0.58) !important;
      transform: translateY(-1px) !important;
    }

    .desktop-nav a[aria-current="page"] {
      color: #0f7773 !important;
      background: rgba(219, 246, 243, 0.72) !important;
      box-shadow: inset 0 0 0 1px rgba(56, 187, 181, 0.11) !important;
      transform: none !important;
    }

    .header-booksy {
      min-height: 46px !important;
      padding: 0 18px !important;
      font-size: 0.82rem !important;
      font-weight: 760 !important;
      letter-spacing: 0.01em !important;
    }
  }

  @media (min-width: 1081px) and (max-width: 1380px) {
    .header-logo-copy strong {
      font-size: 1.02rem !important;
      letter-spacing: 0.055em !important;
    }

    .header-logo-copy em {
      font-size: 0.52rem !important;
      letter-spacing: 0.24em !important;
    }

    .desktop-nav a {
      padding: 0 7px !important;
      font-size: 0.72rem !important;
      font-weight: 680 !important;
    }

    .header-booksy {
      padding: 0 14px !important;
    }
  }

  @media (max-width: 1080px) {
    .mobile-formal-links {
      display: grid !important;
      grid-template-columns: 1fr 1fr !important;
      gap: 10px !important;
      margin-top: 14px !important;
      padding: 0 !important;
      justify-content: center !important;
      align-items: stretch !important;
    }

    .mobile-formal-links a {
      display: flex !important;
      min-height: 44px !important;
      align-items: center !important;
      justify-content: center !important;
      border-radius: 16px !important;
      padding: 0 12px !important;
      color: rgba(23, 21, 29, 0.72) !important;
      font-size: 0.76rem !important;
      font-weight: 720 !important;
      line-height: 1.2 !important;
      text-align: center !important;
      background: rgba(255, 255, 255, 0.68) !important;
      border: 1px solid rgba(183, 101, 118, 0.13) !important;
    }
  }

  @media (max-width: 430px) {
    .mobile-formal-links {
      grid-template-columns: 1fr !important;
    }
  }


  /* V3 — desktop menu: larger, black and more readable */
  @media (min-width: 1081px) {
    .desktop-nav a {
      min-height: 42px !important;
      padding: 0 clamp(10px, 0.78vw, 14px) !important;
      color: rgba(23, 21, 29, 0.92) !important;
      font-size: clamp(0.84rem, 0.78vw, 0.94rem) !important;
      font-weight: 740 !important;
      line-height: 1 !important;
      letter-spacing: -0.01em !important;
    }

    .desktop-nav a:hover {
      color: #17151d !important;
      background: rgba(255, 255, 255, 0.7) !important;
    }

    .desktop-nav a[aria-current="page"] {
      color: #17151d !important;
      background: rgba(219, 246, 243, 0.82) !important;
      box-shadow: inset 0 0 0 1px rgba(56, 187, 181, 0.16) !important;
    }

    .header-booksy {
      font-size: 0.88rem !important;
      font-weight: 800 !important;
    }
  }

  @media (min-width: 1081px) and (max-width: 1380px) {
    .desktop-nav a {
      padding: 0 8px !important;
      font-size: clamp(0.78rem, 0.74vw, 0.86rem) !important;
      font-weight: 730 !important;
    }

    .header-booksy {
      font-size: 0.82rem !important;
      padding: 0 14px !important;
    }
  }

  @media (min-width: 1381px) {
    .desktop-nav {
      gap: 4px !important;
    }
  }


  /* V4 — touch screen polish + larger desktop nav + softer mobile menu subtitle */
  .desktop-nav a,
  .header-booksy,
  .mobile-menu-button,
  .mobile-nav a,
  .mobile-menu-cta a,
  .mobile-formal-links a,
  .header-logo {
    -webkit-tap-highlight-color: transparent !important;
    outline: none;
  }

  .desktop-nav a:focus,
  .desktop-nav a:focus-visible,
  .header-booksy:focus,
  .header-booksy:focus-visible,
  .mobile-menu-button:focus,
  .mobile-menu-button:focus-visible,
  .mobile-nav a:focus,
  .mobile-nav a:focus-visible,
  .mobile-menu-cta a:focus,
  .mobile-menu-cta a:focus-visible,
  .mobile-formal-links a:focus,
  .mobile-formal-links a:focus-visible,
  .header-logo:focus,
  .header-logo:focus-visible {
    outline: none !important;
    box-shadow: none !important;
  }

  @media (hover: hover) and (pointer: fine) {
    .desktop-nav a:focus-visible,
    .header-booksy:focus-visible,
    .mobile-menu-button:focus-visible,
    .mobile-nav a:focus-visible,
    .mobile-menu-cta a:focus-visible,
    .mobile-formal-links a:focus-visible,
    .header-logo:focus-visible {
      box-shadow: 0 0 0 3px rgba(56, 187, 181, 0.18) !important;
    }
  }

  @media (min-width: 1081px) {
    .desktop-nav a {
      min-height: 44px !important;
      padding: 0 clamp(10px, 0.82vw, 15px) !important;
      color: #17151d !important;
      font-size: clamp(0.88rem, 0.82vw, 0.99rem) !important;
      font-weight: 760 !important;
      letter-spacing: -0.014em !important;
    }

    .desktop-nav a:hover {
      color: #17151d !important;
      background: rgba(255, 255, 255, 0.68) !important;
      transform: translateY(-1px) !important;
    }

    .desktop-nav a[aria-current="page"] {
      color: #17151d !important;
      background: rgba(219, 246, 243, 0.78) !important;
      box-shadow: inset 0 0 0 1px rgba(56, 187, 181, 0.12) !important;
    }

    .header-booksy {
      min-height: 48px !important;
      font-size: 0.9rem !important;
      font-weight: 810 !important;
    }
  }

  @media (min-width: 1081px) and (max-width: 1380px) {
    .desktop-nav a {
      padding: 0 8px !important;
      font-size: clamp(0.82rem, 0.78vw, 0.9rem) !important;
      font-weight: 750 !important;
    }

    .header-booksy {
      font-size: 0.84rem !important;
      padding: 0 13px !important;
    }
  }

  @media (max-width: 1080px) {
    .mobile-menu-head {
      padding: 8px 8px 18px !important;
      text-align: center !important;
    }

    .mobile-menu-head p {
      display: inline-flex !important;
      align-items: center !important;
      justify-content: center !important;
      min-height: 30px !important;
      padding: 0 13px !important;
      border-radius: 999px !important;
      background: rgba(219, 246, 243, 0.64) !important;
      color: var(--color-turquoise-dark, #0f7773) !important;
      font-size: 0.7rem !important;
      font-weight: 850 !important;
      letter-spacing: 0.18em !important;
    }

    .mobile-menu-head span {
      max-width: 270px !important;
      margin: 10px auto 0 !important;
      color: rgba(23, 21, 29, 0.74) !important;
      font-size: 1.04rem !important;
      font-weight: 730 !important;
      line-height: 1.28 !important;
      letter-spacing: -0.035em !important;
    }
  }

</style>
